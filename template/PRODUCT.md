# Product

> ⚠ PLACEHOLDERS PRESENT — Claude: if you can read this line, this file hasn't
> been filled in yet. STOP and interview the user (or run `/impeccable init`)
> before any design work — designing from placeholders produces generic slop.
>
> Strategy + brand: who this is for, what it is, what it must feel like.
> Claude reads it before any design work, and the `impeccable` skill reads it
> natively (keep these exact section headings). Fill every field — vague
> input here is the #1 cause of generic output. Easiest way: run
> `/impeccable init` and answer the interview (it writes this file). Delete
> this entire quote block when the file is real.

## Register

<one word — `brand` or `product`. **brand** = design IS the product (landing,
portfolio, campaign, marketing). **product** = design SERVES the product (app
UI, dashboard, tool). This changes how the skills judge every screen.>

## Users

- **Who:** <who actually uses this — role, expertise, device, setting>
- **Context:** <when/where — calm Sunday morning? rushed on the subway? anxious, late at night?>
- **Job to be done:** <the actual outcome they want from each visit>

## Product Purpose

<what this does, why it exists, and a falsifiable success criterion — not
"look professional" but e.g. "a design lead reads a case study and decides to
interview me". Every styling and copy decision gets tested against this.>

## Brand Personality

- **Three words:** <e.g. "warm, mechanical, opinionated" — NOT "modern/clean/elegant", those are dead>
- **Voice / tone:** <how copy should sound; one right/wrong example pair beats adjectives:
  ✅ "Your last backup was 4 days ago." ❌ "Oops! Let's get you back on track ✨">
- **Emotional goal:** <what the interface should make them feel — confidence? calm? delight? urgency?>
- **Naming caveat:** <what the product's NAME wrongly implies, so nobody designs
  from it — e.g. "the product is called Pinky but the brand is midnight navy;
  the name lies on purpose". Leave "none" if the name is honest.>

## Anti-references

<what this must explicitly NOT look like — name the traps for THIS brand:
"not another purple-gradient SaaS", "not a beige+brass artisan cliché",
"not TikTok-loud, not iMovie-toy". 2–3 real products you reject, with why.>

## Design Principles

<3–5 numbered, falsifiable rules — each one testable against a diff. Good:
"One accent. Earn it. (accent color only on the primary action — if it's used
for polish, remove it)" / "Hierarchy through space, not weight" / "Status is
information, not decoration". Bad: "be minimal", "feel premium".>

1. <...>
2. <...>
3. <...>

## Accessibility & Inclusion

- **Targets:** <WCAG level, contrast minimums, reduced-motion, color-blind considerations>
- **Known user needs:** <anything specific to this audience>
